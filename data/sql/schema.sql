CREATE TABLE expositores (id BIGINT AUTO_INCREMENT, nombre text NOT NULL, ocupacion text NOT NULL, email VARCHAR(30) NOT NULL UNIQUE, fono text NOT NULL, titulo text NOT NULL, descripcion text NOT NULL, created_at DATETIME NOT NULL, updated_at DATETIME NOT NULL, PRIMARY KEY(id)) ENGINE = INNODB;
